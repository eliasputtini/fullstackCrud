import styled from "styled-components";

const Main = styled.div`
  height: 100%;
`;
const View = styled.div`
  height: 100%;
`;

const ListView = styled.div`
  display: flex;
  flex-direction: row;
  justify-content: space-evenly;
  flex-wrap: wrap;
`;

export { Main, View, ListView };
